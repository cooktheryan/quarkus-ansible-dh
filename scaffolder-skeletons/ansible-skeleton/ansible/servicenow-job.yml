---
apiVersion: tower.ansible.com/v1alpha1
kind: AnsibleJob
metadata:
  name: servicenow-${{ values.component_id }}
  namespace: janus-argocd
spec:
  connection_secret: aap-access
  job_template_name: "Service Now Change Request"
  extra_vars:
     short_description: "Deploying ${{ values.component_id }} to ${{ values.cluster }}"